"""Setup configuration for FishTracker package.

This minimal setup.py delegates to pyproject.toml for modern Python packaging.
Retained for backward compatibility with older build tools.
"""

from setuptools import setup

if __name__ == "__main__":
    setup()
